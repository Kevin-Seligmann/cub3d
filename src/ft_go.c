#include "cubed.h"
#include "parser.h"

/*
void	ft_go(t_cube *map)
{
	map->ged->mlx = mlx_init();
	map->ged->win = mlx_new_window(map->ged->mlx,
			1400 * map->ged->width, 1500 * map->ged->height, "kseligma & oseivane CUBE3D game");
	//renderizar imagenes
	//rederizar mapa
	mlx_hook(map->ged->win, 17, 0, ft_close, map);
	mlx_key_hook(map->ged->win, ft_key_hook, map);
	mlx_loop(map->ged->mlx);
}
*/
